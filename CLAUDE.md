# CLAUDE.md

このリポジトリで作業する際のルールとメモ。

## 複数PCでの作業ルール（Windows / Mac 両方で開発）
- 作業を始める前に必ず「git pull」で最新を取得してから始める
- 作業を終えたら必ず「git add → commit → push」してから閉じる
- 「1台で作業 → push → もう1台でpull」を徹底し、両方で同時に編集しない
- Claudeは作業開始時に未pushの変更やpull漏れがないか確認する
