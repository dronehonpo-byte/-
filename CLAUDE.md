# 動物顔診断 iOSアプリ — 開発仕様書（CLAUDE.md）

> 出典：株式会社Miyabee 作成「動物顔診断 iOSアプリ 要件定義書 v1.0」（クライアント：大島様）
> このファイルは Claude Code が常時参照する開発仕様書です。
> **実装本体は `AnimalFaceApp/` ディレクトリ配下にあります（本リポジトリは他プロジェクトとの混在リポジトリ）。**

## 0. 基本指示

SwiftUI + StoreKit 2 で iOS ネイティブアプリを新規開発する。以下を厳守：

- **安定性最優先**：クラッシュは致命的。カメラ/写真権限・顔検出失敗・広告読み込み失敗・課金失敗など全ての失敗系を握りつぶさず、ユーザーフレンドリーに継続させる。`try!`・強制アンラップ・未処理 async エラー禁止。
- **マネタイズ最優先**：無料2項目 → 有料4項目への課金導線、広告導線を自然かつ目立つ形で。ダークパターンは避ける。
- **拡張性**：診断項目・動物タイプ・診断文は将来追加される。データ駆動（JSON）＋プロトコル指向で、コード改変なしに項目追加できる設計。
- **未決定事項を確定させない**：アプリ名・カラー・診断文・素材・課金/広告ID は プレースホルダ＋`// TODO(Miyabee):` で明示し、`AppConfig` に集約。本番値を捏造しない。
- **段階実装**：フェーズごとにビルドが通る状態を保つ。
- **プライバシー厳守**：撮影/選択画像は診断後に即時破棄。端末・サーバー・UserDefaults・キャッシュに一切保存しない。

## 1. プロジェクト概要

| 項目 | 内容 |
|---|---|
| 種別 | iOSネイティブアプリ（App Storeリリース） |
| 主機能 | 顔写真を端末内AI（Vision）で顔タイプ（動物等）診断するエンタメアプリ |
| ターゲット | 10代後半〜20代前半 男女 |
| デザイン | シンプル／明るい色味・目立つ・SNS映え |
| マネタイズ | サブスク（初回 月額200円）＋ AdMob広告 |
| 納期方針 | 初期リリースは機能を絞り安定動作優先 |

**医療・専門診断ではない。全診断結果画面に注意書き必須**（「本診断はエンタメ目的であり、医学的・専門的な判定ではありません」）。

## 2. 技術スタック

- Swift 5.9+ / SwiftUI（必要箇所のみ UIKit ブリッジ）、iOS 16.0+
- MVVM、機能ごとフォルダ分割、依存はプロトコル注入
- 顔解析：Vision framework（`VNDetectFaceLandmarksRequest`）→ 独自スコアリング。クラウドAPI（Google/AWS）は `FaceAnalysisProvider` プロトコルの口のみ（初期は未実装）
- 課金：StoreKit 2＋`.storekit` ローカルテスト構成
- 広告：Google Mobile Ads SDK（インタースティシャル＋バナー）。開発中はテスト用 Ad Unit ID
- 分析：匿名利用データのみの `AnalyticsService` 抽象（画像・個人情報は送らない）
- 依存管理：SPM

## 3. 画面構成（全10画面必須）

1. スプラッシュ/トップ（初回はオンボーディング）
2. ユーザー情報入力（性別・年齢、任意・スキップ可）
3. 診断項目一覧（無料2・有料4🔒）
4. 撮影/アップロード（撮影前ガイド付き）
5. 診断中ローディング
6. 診断結果（診断名・%・説明文・動物画像・注意書き・シェア）
7. 広告表示（無料2回目以降の結果表示前インタースティシャル）
8. 課金案内 Paywall
9. 設定（プラン確認・復元購入・問い合わせ・規約リンク）
10. 利用規約/プライバシーポリシー

将来追加（今回実装しない・構造だけ差し込み可能に）：診断履歴／相性診断／招待／ミニゲーム／ランキング／毎日診断／Android。

## 4. 診断仕様

### 診断項目（6項目）

| 診断名 | 区分 | 出力 |
|---|---|---|
| 動物診断（10種） | 無料 | タイプ＋マッチ率% |
| 清楚系度 | 無料 | %＋文 |
| 結婚適正度 | 有料🔒 | %＋文 |
| 醤油顔/ソース顔度 | 有料🔒 | スペクトラム＋文 |
| 童顔度 | 有料🔒 | %＋文 |
| 第一印象好感度 | 有料🔒 | %＋文 |

### 診断ロジック（方針B：特徴に基づく納得感のあるエンタメ診断）

Vision ランドマークから正規化特徴量ベクトルを算出（最低限）：
`faceAspectRatio / eyeSize / eyeSlant / faceWidth / jawSharpness / cheekFullness / mouthSize / featureProminence`

- 動物10タイプは各々ターゲット特徴プロファイル（JSON）を持ち、類似度計算で最も近いタイプ＝結果、類似度＝マッチ率%
- **同一写真は同一結果（決定論・乱数禁止）**。%は特徴に応じて変化（固定値・ランダム禁止）
- プロファイル・閾値は JSON 外部設定でコード改変なしに調整可能

### 動物10タイプ

| # | タイプ | 判定キーワード | 印象ワード |
|---|---|---|---|
| 1 | たぬき顔 | 丸顔・大きく丸い目・頬ふっくら | 優しそう・親しみやすい |
| 2 | きつね顔 | 面長・切れ長の目・鼻筋・シャープ輪郭 | 知的・大人っぽい |
| 3 | 犬顔 | たれ目・柔らかい輪郭・口角上がる | 誠実・話しやすい |
| 4 | 猫顔 | 目尻上がる・小顔・目力・輪郭スッキリ | ミステリアス・個性的 |
| 5 | うさぎ顔 | 大きい目・口小さめ・柔らかい | 可愛い・癒される |
| 6 | リス顔 | 頬ふっくら・大きい目・愛嬌 | 明るい・元気 |
| 7 | 鹿顔 | 面長・縦に大きい目・輪郭細い | 上品・清楚 |
| 8 | 狼顔 | 骨格しっかり・目力強い・顎ライン | かっこいい・頼りになる |
| 9 | ハムスター顔 | 丸顔・頬ふっくら・童顔傾向 | 可愛い・若く見える |
| 10 | ライオン顔 | パーツ大きい・目力強い・存在感 | リーダー的・堂々 |

### 診断文トーン規則（厳守）

褒め表現中心・ユーモアあり・**ネガティブ表現禁止**。動物診断以外は断定しない（「〇〇の印象が強いです」等）。診断文は `diagnosis_content.json` から読込（本番文は Miyabee が後日差し込み）。

### 画像取得

- カメラ＋フォトライブラリ両対応。複数人写る場合は最大の顔を対象
- 顔検出失敗時：フレンドリーなエラー＋再撮影導線（クラッシュ禁止）
- 撮影前ガイド（正面・明るい場所・1人・顔が隠れていない）＋加工/マスク/濃いメイク時の精度低下注意
- **画像は診断後即破棄（不保持）**

## 5. 課金・広告

### 課金（StoreKit 2）
- auto-renewable サブスク。無料＝2項目のみ／課金＝全6項目＋広告非表示＋回数無制限
- 有料項目タップ→🔒→Paywall。復元購入を設定に設置
- **`Transaction.currentEntitlements` を単一の真実源**（自前フラグ禁止）
- 価格改定余地（将来 月額480円・年額4,980円）を見越し設定から拡張可能に

### 広告（AdMob）
- インタースティシャル：無料ユーザー2回目以降の結果表示前
- バナー：一部画面下部（無料のみ）。読み込み失敗時は診断続行（エラー表示なし）
- 「広告を見て結果を見る」／「有料プランで広告なし」の選択UI（後者→Paywall）
- 開発中はテスト Ad Unit ID。本番IDはプレースホルダ

## 6. SNSシェア

- テキスト＋結果画像（`ImageRenderer` で画像化 → 共有シート）
- 画像要素：診断名・%・説明文・動物イラスト・アプリ名・（任意QR）・アイコン
- ハッシュタグ：#顔診断 メイン＋関連（#動物診断 #〇〇顔 #mbti 等）を控えめに
- 優先SNS：TikTok → X → Instagram → LINE（共有シートで対応）

## 7. プライバシー（審査直結・厳守）

- 画像は即時削除（保存なし）。診断履歴は不実装。ユーザー登録は原則不要
- 匿名利用データ（診断回数・広告表示回数・課金クリック数）のみ取得可
- `NSCameraUsageDescription` / `NSPhotoLibraryUsageDescription` を日本語で記載。App Privacy と矛盾しない実装

## 8. スコープ

**IN**：撮影/選択、無料2＋有料4診断、月額200円サブスク、全画面+バナー広告、結果画面、SNSシェア、性別/年齢入力、規約画面、オンボーディング、申請サポート構成。
**OUT**：履歴／相性／招待／ミニゲーム／ランキング／毎日診断／Android。

## 9. プロジェクト構成

```
AnimalFaceApp/
├─ project.yml               # XcodeGen 設定（xcodegen generate で .xcodeproj 生成）
├─ Config.storekit           # ローカル課金テスト
├─ AnimalFaceApp/
│  ├─ App/                   # エントリ、AppState、ルーティング
│  ├─ Core/
│  │  ├─ FaceAnalysis/       # FaceAnalysisProvider, VisionProvider, FaceFeatures
│  │  ├─ Diagnosis/          # ScoringEngine, DiagnosisService, モデル
│  │  ├─ Purchase/           # StoreKit2 EntitlementManager
│  │  ├─ Ads/                # AdMob wrapper（canImport ガード）
│  │  ├─ Share/              # ShareCardView, ShareService
│  │  └─ Analytics/          # AnalyticsService(protocol)
│  ├─ Features/              # 画面ごと View+ViewModel
│  └─ Resources/
│     ├─ Config/             # AppConfig.swift, animal_profiles.json, diagnosis_content.json, hashtags.json
│     ├─ Assets.xcassets/
│     └─ Legal/              # 規約/ポリシー プレースホルダ
└─ Tests/                    # 決定論・スコアリングのユニットテスト
```

## 10. 実装フェーズ

1. 基盤（ナビ骨格・AppConfig・10画面スケルトン）
2. 画像取得（権限・ガイド・失敗系・即破棄）
3. 顔解析＋診断エンジン（無料2項目）
4. 結果画面＋有料4項目
5. 課金（StoreKit2・Paywall・ゲート・復元）
6. 広告（AdMob テストID・選択UI・失敗時継続）
7. SNSシェア
8. オンボーディング/設定/規約
9. 仕上げ（分析フック・安定性・Info.plist・TestFlight準備）

## 11. 未決定リスト（AppConfig に集約・捏造禁止）

`// TODO(Miyabee):` で実装済み：アプリ名／メインカラー・ロゴ／動物PNG素材／診断文（清楚系度・有料4項目）／エラー文言／課金 商品ID・価格／AdMob 本番 Ad Unit ID／規約・ポリシー本文／各種アカウントキー（ビルド設定/環境から注入）

## 12. 受け入れ観点（DoD）

- [ ] 無料2項目が動作、%が写真により変化、同一写真で同一結果
- [ ] 有料4項目：🔒→Paywall→購入で解放、復元購入が機能
- [ ] 課金者は広告非表示、無料2回目以降はインタースティシャル、失敗時も診断継続
- [ ] シェア画像生成→共有シートで各SNSへ
- [ ] 画像がどこにも保存されない
- [ ] 権限拒否・顔検出失敗・ネット断でもクラッシュしない
- [ ] 全結果にエンタメ注意書き、ネガティブ表現なし
- [ ] 未決定値が AppConfig の TODO に集約
- [ ] TestFlight 配布可能なビルド
