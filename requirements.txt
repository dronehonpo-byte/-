# ルート依存パッケージ = 電子契約 Web アプリ (econtract)
#
# Vercel / その他 PaaS のゼロコンフィグ Python ビルドはリポジトリ直下の
# requirements.txt を参照するため、Web アプリの依存をここに集約する。
# 実体は econtract/requirements.txt を参照 (単一のソースに統一)。
-r econtract/requirements.txt

# LINE スクリーンショット監視ツールの依存は requirements-line.txt を参照。
