# LINE スクリーンショット監視ツール 依存パッケージ (Linux / dbus 版)
# pip install -r requirements-line.txt でインストール
#
# ※ Windows 版 (line_screenshot.py) は winsdk + Pillow を使うため、これらは不要です。
#   このファイルは gdbus/D-Bus ベースの Linux デスクトップ通知監視版向けです。

# D-Bus バインディング（Linux デスクトップ通知の受信に必要）
dbus-python>=1.3.2

# GLib メインループ（PyGObject 経由）
PyGObject>=3.42.0

# スクリーンショット取得
Pillow>=10.0.0
